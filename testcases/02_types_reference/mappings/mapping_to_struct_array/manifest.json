{
  "tests": [
    { "file": "001_mapping_to_array_basic.sol", "expect": "pass" },
    {
      "file": "002_mapping_to_array_independent_per_key.sol",
      "expect": "pass",
      "notes": "compile-level check only -- does not verify at runtime that each key's array is genuinely independent"
    },
    { "file": "003_mapping_to_struct_basic.sol", "expect": "pass" },
    {
      "file": "004_mapping_to_struct_containing_array.sol",
      "expect": "pass",
      "notes": "three-way storage composition: mapping key-derived region, struct field layout, array field's own derived region nested inside"
    },
    {
      "file": "005_mapping_to_struct_containing_mapping.sol",
      "expect": "pass",
      "notes": "outer mapping getter is unaffected by the inner struct's mapping field; the Group struct itself remains storage-only/getter-omitted internally"
    },
    {
      "file": "006_array_of_mappings_expected_fail.sol",
      "expect": "fail",
      "notes": "asymmetric with 001: mapping-to-array is legal, but array-of-mappings is not -- mapping is not a valid array element type"
    },
    { "file": "007_mapping_to_fixed_array.sol", "expect": "pass" },
    {
      "file": "008_mapping_to_array_of_structs.sol",
      "expect": "pass",
      "notes": "most compositionally complex fixture in 02_types_reference -- combines mapping key derivation, dynamic array's own derived region, and struct field offsets in one field"
    }
  ]
}